#!/usr/bin/env bash

# 🤖 GitHub Actions 自动部署快速设置

cat << 'EOF'

╔════════════════════════════════════════════════════════════════════════════╗
║                                                                            ║
║              🤖 GitHub Actions 自动部署已配置完成！ 🚀                    ║
║                                                                            ║
╚════════════════════════════════════════════════════════════════════════════╝

📦 已创建文件
══════════════════════════════════════════════════════════════════════════════

✅ .github/workflows/deploy.yml
   ├─ 自动监听 main 分支 push 事件
   ├─ 自动运行构建
   ├─ 自动部署到 Cloudflare Pages
   └─ 已提交到 GitHub

✅ AUTO_DEPLOY_SETUP.md
   └─ 详细配置和使用指南

══════════════════════════════════════════════════════════════════════════════

🔧 3 步启用自动部署
══════════════════════════════════════════════════════════════════════════════

【步骤 1】获取 Cloudflare 凭证
─────────────────────────────────
访问：https://dash.cloudflare.com

1. 右上角 Account Information → 复制 Account ID
2. Account → API Tokens → Create Token
3. 选择预设或自定义权限
4. 复制生成的 API Token

【步骤 2】添加 GitHub Secrets
─────────────────────────────────
访问：https://github.com/AbasswithoutBass/notesview

1. Settings → Secrets and variables → Actions
2. New repository secret
3. 添加 CLOUDFLARE_ACCOUNT_ID
   └─ 值：粘贴 Account ID
4. 添加 CLOUDFLARE_API_TOKEN  
   └─ 值：粘贴 API Token

【步骤 3】测试自动部署
─────────────────────────────────
push 任何代码到 main：

   git add .
   git commit -m "Test auto-deploy"
   git push origin main

然后访问：
   https://github.com/AbasswithoutBass/notesview/actions
   
查看 "Deploy to Cloudflare Pages" 工作流运行状态

══════════════════════════════════════════════════════════════════════════════

✨ 自动部署流程
══════════════════════════════════════════════════════════════════════════════

您的操作                GitHub Actions              Cloudflare
   │                        │                          │
   ├─ git push ────────────→ ├─ Checkout              │
   │  到 main               ├─ Setup Node.js          │
   │                        ├─ Install deps           │
   │                        ├─ Build                  │
   │                        ├─ Deploy ───────────────→ ├─ Publish
   │                        │                         ├─ Generate URL
   │ ←──── 完成通知 ────── ← ← ← ← ← ← ← ← ← ← ← ← ← ← │
   │
   └─ 应用已上线！

时间：通常 2-3 分钟（从 push 到上线）

══════════════════════════════════════════════════════════════════════════════

📊 监控部署
══════════════════════════════════════════════════════════════════════════════

【GitHub Actions 日志】
https://github.com/AbasswithoutBass/notesview/actions
└─ 查看最新的 "Deploy to Cloudflare Pages" 工作流

【Cloudflare Pages 控制面板】
https://dash.cloudflare.com
└─ Pages → notesview → Deployments

══════════════════════════════════════════════════════════════════════════════

💡 使用场景
══════════════════════════════════════════════════════════════════════════════

场景 1：修复 bug
  git add .
  git commit -m "Fix: audio not playing on iOS"
  git push origin main
  ✓ 自动部署到 Cloudflare
  ✓ 用户立即看到修复

场景 2：添加新功能
  git add .
  git commit -m "Add: difficulty level preview"
  git push origin main
  ✓ 自动部署到 Cloudflare
  ✓ 新功能立即上线

场景 3：更新文档或样式
  git add .
  git commit -m "Docs: update README"
  git push origin main
  ✓ 自动部署到 Cloudflare
  ✓ 无需手动操作

══════════════════════════════════════════════════════════════════════════════

🎯 配置完成后的流程
══════════════════════════════════════════════════════════════════════════════

1️⃣  正常开发，在本地修改代码
2️⃣  提交到 Git：git add . && git commit -m "message"
3️⃣  推送到 main：git push origin main
4️⃣  GitHub Actions 自动触发（2-3 分钟内完成部署）
5️⃣  应用自动更新到最新版本

【无需手动执行任何命令！】

══════════════════════════════════════════════════════════════════════════════

🔐 安全提示
══════════════════════════════════════════════════════════════════════════════

✓ API Token 安全存储在 GitHub Secrets
✓ Token 不会暴露在日志中
✓ 只有推送到 main 分支时才触发部署
✓ 可随时撤销 API Token（Cloudflare 控制面板）
✓ GitHub 组织成员可见 Secrets 名称但不能看到值

══════════════════════════════════════════════════════════════════════════════

📚 详细文档
══════════════════════════════════════════════════════════════════════════════

查看完整配置指南：
$ cat AUTO_DEPLOY_SETUP.md

═══════════════════════════════════════════════════════════════════════════════

🎉 准备就绪！
═══════════════════════════════════════════════════════════════════════════════

按照上述 3 步配置后，您的 NotesView 应用将：
✓ 自动构建
✓ 自动部署到 Cloudflare Pages
✓ 每次 push 都立即更新

再也不需要手动运行 wrangler 命令了！

═══════════════════════════════════════════════════════════════════════════════

📞 需要帮助？
═══════════════════════════════════════════════════════════════════════════════

查看 AUTO_DEPLOY_SETUP.md 中的常见问题部分

═══════════════════════════════════════════════════════════════════════════════

                    祝自动部署顺利！🚀 🎵 ✨

═══════════════════════════════════════════════════════════════════════════════

EOF
